<div class="container">
  <div class="row d-flex justify-content-center mt-5">
    <div class="col-6">
      <%= simple_form_for [@list, @review] do |f| %>
        <%= f.input :content %>
        <%= f.button :submit, class: 'btn btn-gradient w-100'  %>
      <% end %>
    </div>
  </div>
</div>
